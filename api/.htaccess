<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Handle OPTIONS method to return 200 OK immediately
    # allowing the browser to see the CORS headers from server config
    RewriteCond %{REQUEST_METHOD} OPTIONS
    RewriteRule ^(.*)$ $1 [R=200,L]
</IfModule>
